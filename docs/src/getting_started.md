# Getting Started

1. Include the package module from `src/SubBottomProfiler.jl`.
2. Read or generate a `Dataset`.
3. Build a `ProcessingPipeline` or load a TOML workflow.
4. Run interpretation or export steps.
